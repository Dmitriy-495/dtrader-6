# =============================================================================
# DTrader 6 — WS Server Configuration
# =============================================================================

app:
  name: dtrader-6-ws-server
  env: development

server:
  port: 9000

symbols:
  - BTC_USDT
  - ETH_USDT
  - SOL_USDT

redis:
  host: localhost
  port: 6379
  db: 0
